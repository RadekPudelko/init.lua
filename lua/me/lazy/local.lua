return {
    "particle",
    dir="~/.config/particle.nvim",
    -- 'RadekPudelko/particle.nvim'
    config = function()
        -- local particle = require("particle")
        require("particle")
        -- particle.setup()
    end
}
